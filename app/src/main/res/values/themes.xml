<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieHiltModular" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MovieHiltModular" parent="Base.Theme.MovieHiltModular" />

    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="colorPrimary">@color/gr_en_co</item>
        <item name="colorPrimaryDark">@color/gr_st_co</item>
        <item name="colorAccent">@color/gr_en_co</item>
        <item name="android:statusBarColor">@color/gr_st_co</item>
    </style>
</resources>